# Bot Observatory companion

Route: `/workspace/bot-activity/`
Demo: `/workspace/bot-activity/?demo=1`

```bash
git fetch origin
git checkout bot-activity-companion
cd web && npm i && npm run dev
```

Open the demo URL first. Live bots need the hub and a signed-in workspace.
The live site is not updated until this branch is deployed.
